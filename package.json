{
  "name": "rreal-tacos-ocr-form",
  "version": "1.0.0",
  "description": "OCR form for Rreal Tacos invoice processing",
  "scripts": {
    "dev": "concurrently \"vercel dev\" \"cd client && npm start\"",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "openai": "^4.20.1",
    "multer": "^1.4.5-lts.1",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vercel": "^32.3.0"
  }
} 